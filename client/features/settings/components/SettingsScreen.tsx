import { AccountButton } from "@/features/auth";
import { View } from "react-native";

export const SettingsScreen = () => {
  return (
    <View className="p-3">
      <AccountButton />
    </View>
  );
};
